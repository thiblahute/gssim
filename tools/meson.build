executable('gssim-compare-images',
  'gssim-compare-images.c',
  install: true,
  dependencies : [glib_dep, gdk_pixbuf_dep, gst_dep, gst_video_dep, gobject_dep],
  link_args : extra_libs,
  link_with : [gssim]
)

